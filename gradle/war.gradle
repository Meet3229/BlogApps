apply plugin: "war"

bootWar {
    mainClass = "com.mycompany.myapp.BlogAppsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
